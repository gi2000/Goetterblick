package general;

import data.consts.ConstTranslation;
import utils.handler.TranslationHandler;

public class Start
{
    public static void main(String[] args)
    {
        // Workaround for a still unfixed bug in JavaFX.
        Initialisation.main(args);
    }
}
